export const exportSoldItemsToCSV = (soldItems: any[], periodLabel: string, currency: string, showCostData: boolean) => {
  console.log('Exporting to CSV:', soldItems, periodLabel, currency, showCostData);
};
